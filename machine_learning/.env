# playercore_retain/machine_learning/.env

# Environment
NODE_ENV=development

# Database Configuration (PostgreSQL Local Docker)
# Using local Docker PostgreSQL on port 5434 with synthetic data
# For AWS connection: Use port 5433 (SSH tunnel to EC2)
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=playercore123
DB_DATABASE=playercore_retain